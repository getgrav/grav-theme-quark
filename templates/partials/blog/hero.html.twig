<section id="blog-hero" class="section hero {{ page.header.hero_classes }} {{ page.header.background.parallax ? 'parallax' : '' }}" {% if blog_image %}style="background-image: url({{ blog_image.url }});"{% endif %}>
    <div class="image-overlay"></div>
    <section class="container {{ grid_size }}" style="text-align: {{ page.header.text_align|default('left') }}">
        {{ content }}
    </section>
    <i id="to-start" class="fa fa-angle-down"></i>
</section>